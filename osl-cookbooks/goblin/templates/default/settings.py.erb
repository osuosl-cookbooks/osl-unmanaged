# Django settings for goblin project.

DEBUG = True
TEMPLATE_DEBUG = DEBUG

ADMINS = (
    ('<% node['goblin']['django']['adminname']%>', '<% node['goblin']['django']['adminmail']%>'),
)

MANAGERS = ADMINS

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2', # Add 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'oracle'.
        'NAME':'<% node['goblin']['django']['dbname'] %>',                      # Or path to database file if using sqlite3.
        'USER': '<% node['goblin']['django']['username'] %>',                      # Not used with sqlite3.
        'PASSWORD': '<% node['goblin']['django']['password']%>',                  # Not used with sqlite3.
        'HOST': '',                      # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
    }
}
