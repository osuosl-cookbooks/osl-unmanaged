{
  "env_run_lists": {},
  "run_list": [
    "role[base_managed]",
    "recipe[osl-openstack::compute]",
    "recipe[firewall::iscsi]",
    "recipe[firewall::openstack]",
    "recipe[firewall::rsync]"
  ],
  "chef_type": "role",
  "override_attributes": {
    "authorization": {
      "sudo": {
        "include_sudoers_d": true
      }
    }
  },
  "default_attributes": {},
  "json_class": "Chef::Role",
  "description": "compute role to setup ports, packages, sudoers for an openstack compute node",
  "name": "openstack_compute"
}
