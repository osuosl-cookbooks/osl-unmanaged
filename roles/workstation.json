{
  "name": "workstation",
  "description": "Role for OSL Student Workstations",
  "json_class": "Chef::Role",
  "default_attributes": { },
  "override_attributes": {
    "authorization": {
      "sudo": {
        "users": ["osuadmin"],
        "groups": []
      }
    }
  },
  "chef_type": "role",
  "run_list": [
    "recipe[base::users]",
    "recipe[sudo::default]",
    "recipe[workstation::ldap]",
    "role[workstation_base]",
    "recipe[workstation::network]",
    "recipe[workstation::sudoers]",
    "recipe[workstation::autofs]",
    "recipe[workstation::users]",
    "recipe[workstation::vimrc]",
    "recipe[workstation::scripts]",
    "recipe[base::grub]",
    "recipe[workstation::systemd]"
  ],
  "env_run_lists": {
  }
}
